# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript incremental build info (created by `tsc -b`)
*.tsbuildinfo

# Test coverage output (created by `npm run test:coverage`)
coverage

# Environment — see .env.example for the schema
.env
.env.local
.env.*.local

# Private plugin slot — mounted as a submodule in private forks (e.g. the AI
# coaching plugin). Absent in the public repo and on Lovable; the plugin loader
# discovers it only when present. See src/plugins/README.md.
src/plugins/coaching/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
