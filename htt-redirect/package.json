{
  "name": "htt-redirect",
  "version": "1.0.0",
  "private": true,
  "description": "Redirect Worker sending hackthetrack.net (the old domain) to lapwingdata.com",
  "scripts": {
    "deploy": "wrangler deploy --config ./wrangler.toml",
    "dry-run": "wrangler deploy --dry-run --config ./wrangler.toml"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "wrangler": "^4.20.0"
  }
}
